# Server (MCP + REST sulla stessa porta)
MCP_PORT=3001
MCP_AUTH_TOKEN=cambiami-con-un-token-sicuro
MCP_SESSION_TTL=600000

# URL pubblico della VPS (usato per generare i link alle immagini)
REST_BASE_URL=https://tuodominio.it
